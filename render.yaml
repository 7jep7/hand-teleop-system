services:
  - type: web
    name: hand-teleop-api
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.web_api:app --host 0.0.0.0 --port $PORT
    plan: free
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.18
